# Backend environment variables (local development)
# Use localhost when running the Spring Boot app on your host machine.
DB_URL=jdbc:postgresql://localhost:5432/sportsms
DB_USERNAME=sportsms
DB_PASSWORD=sportsms

# JWT configuration
JWT_SECRET=dev-secret-key-change

# Upload configuration
UPLOAD_DIR=uploads
UPLOAD_BASE_URL=http://localhost:8080/uploads
